[project]
name = "deha-drama-scraper"
version = "0.0.1"
description = "TUI Drama Scraper with fzf and MPV support"
dependencies = [
    "requests",
    "beautifulsoup4",
    "flask>=3.1.3",
    "serverless-wsgi>=3.1.0",
]

[project.scripts]
deha = "main:main"
