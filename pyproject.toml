[project]
name = "deha-drama-scraper"
version = "0.0.1"
description = "TUI Drama Scraper with fzf and MPV support"
dependencies = [
    "requests",
    "beautifulsoup4",
]

[project.scripts]
deha = "main:main"
